---
trigger: always_on
glob:
description: Design prototype is the source of truth for all visual changes
---

All visual/design changes MUST start from the prototype files in `docs/design/`.
The latest adopted prototype is the single source of truth for the site's appearance.
Never modify styling in `src/` without first validating the design in the prototype.
