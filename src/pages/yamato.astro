---
import fs from "node:fs";
import path from "node:path";

const htmlPath = path.resolve("public/works/yamato/B.html");
const html = fs.readFileSync(htmlPath, "utf-8");
---

<Fragment set:html={html} />
